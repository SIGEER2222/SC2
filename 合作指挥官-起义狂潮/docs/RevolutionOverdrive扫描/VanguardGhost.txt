■ [VanguardGhost]  (Attrs=Biological+Psionic)
  ├─ 能力 AbilArray:
  │  • CAbilStop(stop) → 卡牌[Stop(Stop)]
  │  • CAbilAttack(attack) → 卡牌[Execute(AttackGhost)]
  │  • CAbilMove(move) → 卡牌[Move(Move), HoldPos(MoveHoldPosition), Patrol(MovePatrol)]
  │  • CAbilBehavior(GhostCloak) → 卡牌[On(CloakOnGhost), Off(CloakOff)]
  │  • CAbilEffectTarget(TacNukeStrike) → 卡牌[Cancel(Cancel), Execute(NukeCalldown)]
  │  • CAbilEffectInstant(GhostHoldFire) → 卡牌[Execute(GhostHoldFire)]
  │  • CAbilEffectInstant(GhostWeaponsFree) → 卡牌[Execute(WeaponsFree)]
  │  • CAbilEffectTarget(HighIntensityEMP) → 卡牌[Execute(HighIntensityEMP)]
  │  • CAbilEffectTarget(PsiHijack) → 卡牌[Execute(PsiHijack)]
  ├─ 卡牌按钮（无 AbilCmd）:
  │  • [R1C1] VulkanClassCombatSuit
  │  • [R1C0] MoebiusPsiProjector
  ├─ 武器: NovaWeaponHellfireShotgun
  ├─ ← 被生产由:
  │  • [Barracks] via BarracksTrain[Train12]