## 斯旺

- 扫描日期：2026-06-21
- 指挥官：TerranSwann

### 扫描结果

- P1 `重武器专家`：仍然吃到原版缺点，`DrakkenLaserDrillConcentratedBeamIssueOrder` 在 `UserData.xml` 中继续被 `DisableAbil` 锁掉。
- 同一条威望下，`DrakkenLaserDrillPulseCannonIssueOrder` 已由当前生成层回补，不是这次的残留点。

### 核心改动

- 文件：[LibE0EAE146_SwannRuntime.galaxy](/E:/Code/MyMod/SC2/合作指挥官-起义狂潮/Mods/7vs1/CoopZeroPop.SC2Mod/Base.SC2Data/LibE0EAE146_SwannRuntime.galaxy)
- 新增 `libE0EAE146_gf_SwannApplyPrestigePenaltyBypass`。
- 在斯旺运行时初始化时，如果当前是 P1，则重新放开：
  - `DrakkenLaserDrillConcentratedBeamIssueOrder`

### 验证

- 实际启动游戏验证 `TerranSwann` P1，显式指定原始 replay extract `SourceRoot`。
- 检查 `C:\Users\22448\Documents\StarCraft II\GameLogs`，本轮验证后未生成新的 `*ScriptError*.log`。
