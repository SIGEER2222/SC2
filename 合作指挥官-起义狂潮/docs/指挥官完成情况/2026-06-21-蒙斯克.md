## 蒙斯克

- 扫描日期：2026-06-21
- 指挥官：TerranMengsk

### 扫描结果

- P1 `帝国炮火`：仍然吃到原版缺点，`NuclearAnnihilationMengsk` 被 `UserData.xml` 里的 `DisableAbil` 锁掉。
- P2 `王家卫队`：当前本地数据未继续看到额外锁单位/锁技能负面，运行时只保留正面经验加成逻辑。
- P3 `装备精良`：仍然吃到原版缺点，`FusionCoreMengskResearch`、`ArmoryMengskResearch` 和 `MedivacMengsk` 被 `UserData.xml` 锁掉。

### 核心改动

- 文件：[LibE0EAE146_MengskRuntime.galaxy](/E:/Code/MyMod/SC2/合作指挥官-起义狂潮/Mods/7vs1/CoopZeroPop.SC2Mod/Base.SC2Data/LibE0EAE146_MengskRuntime.galaxy)
- 新增 `libE0EAE146_gf_MengskApplyPrestigePenaltyBypass`。
- 在蒙斯克运行时初始化时，按威望掩码回补被负面锁掉的内容：
  - P1 重新放开 `NuclearAnnihilationMengsk`
  - P3 重新放开 `FusionCoreMengskResearch`、`ArmoryMengskResearch`、`MedivacMengsk`
- 文件：[LibKMIS.galaxy](/E:/Code/MyMod/SC2/合作指挥官-起义狂潮/Mods/7vs1/CoopZeroPop.SC2Mod/Base.SC2Data/LibKMIS.galaxy)
- 在 `libKMIS_gf_CM_Mengsk_EnergyChanged()` 里加了空值保护，避免 `cU_GPEnergyBar` 未初始化时触发 `DialogControlSetPropertyAsFixed` 报错。
- 同步给两个回填文本入口加了 `<= 0` 保护，避免 `DialogControlLastCreated()` 为 0 时继续调用 `SetDialogItemText`。

### 验证

- 实际启动游戏验证 `TerranMengsk` P1。
- 实际启动游戏验证 `TerranMengsk` P3。
- 检查 `C:\Users\22448\Documents\StarCraft II\GameLogs`，本次两轮验证后均未生成新的 `*ScriptError*.log`。
