## 斯台特曼

- 扫描日期：2026-06-21
- 指挥官：ZergStetmann

### 扫描结果

- P1 `斯台特卫星专家`：仍然吃到原版缺点，`MorphToSuperGaryStetmann` 在 `UserData.xml` 中继续被 `DisableAbil` 锁掉。
- P3 `战斗增益协议`：仍然吃到原版缺点，`InfestationPitStetmannResearch2` 的两个命令位在 `UserData.xml` 中继续被 `DisableAbil` 锁掉。
- 当前生成层只会补对应威望升级，不会把这两组被禁用的能力命令重新放开，所以会残留锁技能问题。

### 核心改动

- 文件：[LibE0EAE146_StetmannRuntime.galaxy](/E:/Code/MyMod/SC2/合作指挥官-起义狂潮/Mods/7vs1/CoopZeroPop.SC2Mod/Base.SC2Data/LibE0EAE146_StetmannRuntime.galaxy)
- 新增 `libE0EAE146_gf_StetmannApplyPrestigePenaltyBypass`。
- 在斯台特曼运行时初始化时按威望位重新放开：
  - P1：`MorphToSuperGaryStetmann`
  - P3：`InfestationPitStetmannResearch2` 命令位 `0`、`1`

### 验证

- 实际启动游戏验证 `ZergStetmann` P1。
- 实际启动游戏验证 `ZergStetmann` P3。
- 检查 `C:\Users\22448\Documents\StarCraft II\GameLogs`，本轮验证后未生成新的 `*ScriptError*.log`。
