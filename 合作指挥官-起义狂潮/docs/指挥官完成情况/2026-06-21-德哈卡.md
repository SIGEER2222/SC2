## 德哈卡

- 扫描日期：2026-06-21
- 指挥官：ZergDehaka

### 扫描结果

- P1 `吞噬者`：仍然吃到原版缺点，`DehakaLearn` 的 `Cmd=3` 和 `MammothBreath` 的施法命令位在 `UserData.xml` 中继续被 `DisableAbil` 锁掉。
- 当前生成闭包虽然已经回补了部分 `DehakaLearn` 命令位，但没有覆盖 `Cmd=3` 和 `MammothBreath`，所以会残留“吞食/灼热吐息被锁”的问题。

### 核心改动

- 文件：[LibE0EAE146_DehakaRuntime.galaxy](/E:/Code/MyMod/SC2/合作指挥官-起义狂潮/Mods/7vs1/CoopZeroPop.SC2Mod/Base.SC2Data/LibE0EAE146_DehakaRuntime.galaxy)
- 新增 `libE0EAE146_gf_DehakaApplyPrestigePenaltyBypass`。
- 在德哈卡运行时初始化时，如果当前是 P1，则重新放开：
  - `DehakaLearn` 命令位 `3`
  - `MammothBreath` 命令位 `0`

### 验证

- 实际启动游戏验证 `ZergDehaka` P1。
- 检查 `C:\Users\22448\Documents\StarCraft II\GameLogs`，本轮验证后未生成新的 `*ScriptError*.log`。
