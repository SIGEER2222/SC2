■ 虫后 [Queen]  (Attrs=Biological+Psionic, 科技树:锁定)
  ├─ 能力 AbilArray:
  │  • CAbilStop(stop) → 卡牌[Stop(Stop)]
  │  • CAbilAttack(attack) → 卡牌[Execute(Attack)]
  │  • CAbilMove(move) → 卡牌[Move(Move), HoldPos(MoveHoldPosition), Patrol(MovePatrol)]
  │  • CAbilBuild(QueenBuild) → 卡牌[Build1(BuildCreepTumor), Build3(BuildCreepTumor)]
  │  • CAbilMorph(BurrowQueenDown) → 卡牌[Execute(BurrowDown)]
  │  • CAbilEffectTarget(SpawnLarva)
  │  • CAbilEffectTarget(Transfusion)
  │  • CAbilQueue(que1)
  │  • CAbilTrain(EvolveToBrutaliskQueen) → 卡牌[Train1(进化为莽兽)]
  │  • CAbilEffectTarget(BioMechanicalTransfusion) «生物机械哺液» → 卡牌[Execute(速效哺液)]
  │  • ?(DeepTunnel) → 卡牌[Execute(生物质搜集)]
  │  • ?(KerriganEnhance) → 卡牌[Off(Cancel)]
  │  • CAbilEffectTarget(QueenBurstHeal)
  │  • CAbilMorph(BurrowBanelingDown)
  │  • CAbilMorph(BurrowHydraliskDown)
  │  • CAbilMorph(BurrowRavagerDown)
  │  • CAbilMorph(MorphToSwarmHostBurrowedMP)
  │  • CAbilMorph(BurrowInfestorTerranDown)
  │  • CAbilMorph(BurrowInfestorDown)
  │  • CAbilMorph(BurrowUltraliskDown) → 卡牌[Execute()]
  ├─ 卡牌按钮（无 AbilCmd）:
  │  • [R2C3] 生物质搜集
  │  • [R1C1] 生物机械哺液
  ├─ 可建造建筑 (Build):
  │  • [CreepTumorQueen] {t=15s, 图标=CreepTumor}
  ├─ 可变形为 (Morph):
  │  • [QueenBurrowed] via CAbilMorph(BurrowQueenDown)
  │  • [BanelingBurrowed] via CAbilMorph(BurrowBanelingDown)
  │  • [HydraliskBurrowed] via CAbilMorph(BurrowHydraliskDown)
  │  • [RavagerBurrowed] via CAbilMorph(BurrowRavagerDown)
  │  • [SwarmHostBurrowedMP] via CAbilMorph(MorphToSwarmHostBurrowedMP)
  │  • [InfestorTerranBurrowed] via CAbilMorph(BurrowInfestorTerranDown)
  │  • [InfestorBurrowed] via CAbilMorph(BurrowInfestorDown)
  │  • [UltraliskBurrowed] via CAbilMorph(BurrowUltraliskDown)
  ├─ 武器: AcidSpines, Talons, TalonsMissile
  ├─ ← 被生产由:
  │  • [Hatchery], [Lair], [Hive] via TrainQueen[Train1]
  ├─ ← 被变异由:
  │  • [QueenBurrowed] via BurrowQueenUp
  │  • ?(QueenLand) via QueenLand