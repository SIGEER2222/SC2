■ [DefilerMPReborn]  (parent=DefilerMP, Attrs=Biological+Psionic, 科技树:已解锁)
  ├─ 能力 AbilArray:
  │  • CAbilEffectInstant(AbathurCasterBrainPoolPurchaseReborn) → 卡牌[Execute(增殖脑池)]
  │  • CAbilEffectInstant(AbathurCasterNeuralTethersPurchaseReborn) → 卡牌[Execute(远距神经束)]
  │  • CAbilEffectInstant(AbathurCasterFleshReactorPurchaseReborn) → 卡牌[Execute(血肉转炉)]
  │  • CAbilEffectInstant(AbathurCasterMimeticMembranePurchaseReborn) → 卡牌[Execute(拟态隐膜)]
  │  • CAbilStop(stop) → 卡牌[Stop(Stop)]
  │  • CAbilMove(move) → 卡牌[Move(Move), HoldPos(MoveHoldPosition), Patrol(MovePatrol), AcquireMove(AcquireMove)]
  │  • CAbilEffectTarget(DefilerMPConsume) → 卡牌[Execute(DefilerMPConsume)]
  │  • CAbilEffectTarget(DefilerMPDarkSwarm) → 卡牌[Execute(DefilerMPDarkSwarm)]
  │  • CAbilEffectTarget(DefilerMPPlague) → 卡牌[Execute(DefilerMPPlague)]
  │  • CAbilMorph(DefilerMPBurrow) → 卡牌[Execute(BurrowDown)]
  │  • CAbilEffectTarget(InfestorConsumption)
  │  • CAbilTrain(EvolveToBrutaliskDefiler) → 卡牌[Train1(进化为莽兽)]
  │  • CAbilQueue(que1)
  │  • CAbilAttack(attack)
  ├─ 可变形为 (Morph):
  │  • [DefilerMPBurrowed] via CAbilMorph(DefilerMPBurrow)