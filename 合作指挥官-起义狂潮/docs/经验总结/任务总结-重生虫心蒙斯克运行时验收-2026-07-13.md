# 任务总结：重生虫心蒙斯克运行时验收

- 任务类型：SC2 Reborn 兼容移植运行时验收
- 时间戳：2026-07-13 18:52:49 +08:00
- 任务内容：在 `zexpedition03_reborn_port.SC2Map` 中真实启动 `TerranMengsk`，确认指挥官替换、地图加载、运行时单位和 Neuro RuntimeProbe 事件。
- 任务结果：地图进入任务阶段，SC2 进程（PID 2988）持续存活。RuntimeProbe 确认 `CommandCenterMengsk`、`BarracksMengsk`、`SCVMengsk`、`CoopCasterMengsk` 和 `MengskCommander` 均已生效，种族替换判定为 `SUCCESS`。但不能作为完整可玩验收：GameLogs 新增 `TechTreeUpgradeCount` 玩家槽位 16 越界错误；Probe 的 65 个允许能力和科技项仍是雷诺 ID，说明单位/科技生产链或采样范围存在跨指挥官残留。
- 任务耗时：约 4 分钟
- 任务备注：启动命令为 `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/reborn/launch-reborn-commander.ps1 -Commander TerranMengsk -MapName zexpedition03_reborn_port.SC2Map -EnableRuntimeProbe -ProbeDuration 60`。RuntimeProbe 服务 verdict 显示 pass，但与同局 `ScriptError.txt` 矛盾；后续修复前不得以该 verdict 单独证明无 ScriptError。
