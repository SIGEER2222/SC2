# 按指挥官过滤生成单位到测试地图

**日期**: 2026-07-05
**任务**: 在阿巴瑟之心测试地图中支持 `-Commander` 参数，只生成指定指挥官的单位到地图中央

## 背景

测试地图 `AbathurHeartTest_unpacked` 默认会把 25 个指挥官组、663 个单位全部生成到地图上，单位过多不便逐个查看。需要支持按指挥官 ID 过滤，单独生成某个指挥官的单位到地图中央。

## 实现方案

### 全局变量 + 启动脚本正则替换（不动源文件）

**MapScript.galaxy 端**：
```galaxy
string gv_commanderFilter = "";

bool gf_CommanderMatchesFilter (string identifier, string filter) {
    if (filter == "") { return true; }
    if (StringFind(identifier, filter, false) > 0) { return true; }
    return false;
}
```

**test-abathur-heart.ps1 端**：
```powershell
function Set-CommanderFilter {
    param([string]$MapScriptPath, [string]$Commander)
    $content = [System.IO.File]::ReadAllText($MapScriptPath)
    $pattern = 'gv_commanderFilter\s*=\s*"[^"]*"\s*;'
    $replacement = 'gv_commanderFilter = "' + $Commander + '";'
    $newContent = [regex]::Replace($content, $pattern, $replacement)
    [System.IO.File]::WriteAllText($MapScriptPath, $newContent, [System.Text.UTF8Encoding]::new($false))
}

# 在同步地图后调用，只改 mapLive 下的副本，不动源文件
if ($Commander -ne "") {
    Set-CommanderFilter -MapScriptPath (Join-Path $mapLive "MapScript.galaxy") -Commander $Commander
}
```

## 关键坑

### 坑 1: `StringCase` 在 mod 环境下编译报错

**现象**：调用 `StringCase(filter, true)` 报错：
```
Script compile error: MapScript.galaxy (80), 参数类型同函数定义不匹配
```

**原因**：虽然 `natives.galaxy` 中 `StringCase` 签名是 `(string s, bool upper)`，但 VioletsHoTSReworkMod 等 mod 替换了 NativeLib，导致该函数不可用或签名改变。

**修复**：改用 `StringFind(identifier, filter, false)`（第三个参数 `false` = 不区分大小写），返回值 > 0 即包含匹配。一行代码搞定，更简洁。

### 坑 2: PowerShell heredoc 不支持

**现象**：`git commit -m "$(cat <<'EOF' ... EOF)"` 在 PowerShell 中报错：
```
Missing file specification after redirection operator.
```

**修复**：用 PowerShell here-string `@'...'@` 或 `@"..."@`，再传给 `-m` 参数：
```powershell
$msg = @'
commit 说明第一行
commit 说明第二行
'@
git commit -m $msg
```

### 坑 3: trae-add.ps1 包装脚本不存在

**现象**：AGENTS.md 列出了 `scripts/trae-add.ps1` 包装脚本，但实际仓库中不存在该文件。

**修复**：直接用 `git add "具体文件路径"`，明确指定文件，符合 AGENTS.md「禁止使用 `git add .` / `git add -A`」的要求。

## 布局策略

- **无过滤**（生成全部 25 组）：从左上角 (4, 124) 开始，5 列 × N 行铺开
- **有过滤**（匹配 1 组或几组）：从地图中央偏上 (30, 110) 开始，方便查看
- 过滤后重新排列位置，不留空位（即匹配的第 2 个组不会被错位到第 N 列）

## 用法示例

```powershell
# 只生成阿巴瑟（34 个单位）
.\test-abathur-heart.ps1 -Commander "Cmdr_Abathur"

# 只生成 Violets 的凯瑞甘子组
.\test-abathur-heart.ps1 -Commander "Violets_Kerrigan"

# 前缀匹配，生成所有 Violets 子组（10 个组）
.\test-abathur-heart.ps1 -Commander "Violets_"

# 不指定 = 生成全部 25 个组（原行为）
.\test-abathur-heart.ps1
```

## 验证结果

`-Commander "Cmdr_Abathur"` 测试：
- 加载耗时：43 秒
- ScriptError：无关键错误
- 生成位置：(30, 110) 起的 5 列 × 5 行区域
- 生成单位数：34 个（与扫描报告一致）

## 可用 commander identifier 清单

| 类型 | identifier | 标签 |
|------|-----------|------|
| 单指挥官 mod | `Cmdr_Dehaka` | Dehaka（迪哈卡） |
| 单指挥官 mod | `Cmdr_Ihanrii` | Ihanrii（伊汉里） |
| 单指挥官 mod | `Cmdr_Moebius` | Moebius（莫比斯） |
| Alenger 系列 | `Cmdr_Ironclad` | Ironclad（钢铁） |
| Alenger 系列 | `Cmdr_Behemoth` | Behemoth（贝希摩斯虫群） |
| Alenger 系列 | `Cmdr_MadEmpire` | MadEmpire（疯批帝国） |
| Alenger 系列 | `Cmdr_Taldarim` | Taldarim（塔达林） |
| Alenger 系列 | `Cmdr_Abathur` | Abathur（阿巴瑟） |
| Alenger 系列 | `Cmdr_Khalai` | Khalai（卡莱） |
| Alenger 系列 | `Cmdr_Zagara` | Zagara（扎加拉） |
| Alenger 系列 | `Cmdr_Pirate` | Pirate（海盗） |
| Alenger 系列 | `Cmdr_Amon` | Amon（埃蒙） |
| Alenger 系列 | `Cmdr_Friends` | Friends（群友） |
| Alenger 系列 | `Cmdr_Ranger` | Ranger（游骑兵） |
| Alenger 系列 | `Cmdr_Purifier` | Purifier（净化者） |
| Violets 子组 | `Violets_Mengsk` | Violets/Mengsk（蒙斯克） |
| Violets 子组 | `Violets_Dehaka` | Violets/Dehaka（迪哈卡） |
| Violets 子组 | `Violets_Stukov` | Violets/Stukov（斯图科夫） |
| Violets 子组 | `Violets_Kerrigan` | Violets/Kerrigan（凯瑞甘） |
| Violets 子组 | `Violets_ZaGara` | Violets/ZaGara（扎加拉） |
| Violets 子组 | `Violets_Artanis` | Violets/Artanis（阿塔尼斯） |
| Violets 子组 | `Violets_Zeratul` | Violets/Zeratul（泽拉图） |
| Violets 子组 | `Violets_Alarak` | Violets/Alarak（阿拉纳克） |
| Violets 子组 | `Violets_Fenix` | Violets/Fenix（菲尼克斯） |
| Violets 子组 | `Violets_Shared` | Violets/Shared（共享基础单位） |

## 相关文件

- [MapScript.galaxy](file:///E:/Code/MyMod/SC2/合作指挥官-起义狂潮/Maps/AbathurHeartTest_unpacked/MapScript.galaxy)
- [test-abathur-heart.ps1](file:///E:/Code/MyMod/SC2/合作指挥官-起义狂潮/scripts/test-abathur-heart.ps1)
- [LibAbathurHeartCatalog.galaxy](file:///E:/Code/MyMod/SC2/合作指挥官-起义狂潮/Maps/AbathurHeartTest_unpacked/Base.SC2Data/LibAbathurHeartCatalog.galaxy)
- [_gen_abathur_heart_catalog.py](file:///E:/Code/MyMod/SC2/合作指挥官-起义狂潮/scripts/_gen_abathur_heart_catalog.py)
