# 三项主线任务进度报告

> 时间戳：2026-07-12 约 06:56（Asia/Shanghai）  
> 任务类型：只读审阅 + 文档总结  
> 任务内容：分别总结 1）合作指挥官独立 Mod 化；2）Neuro 集成；3）Galaxy 工具迁移。  
> 任务结果：已完成阶段性进度报告；未修改地图、Mod、启动器或运行时产物。  
> 任务耗时：约 20 分钟。  
> 任务备注：本报告基于仓库文件、git 状态、DataCenter/Galaxy 检查和工具构建测试结果；当前工作区存在多处其他任务脏改动，未纳入本报告提交范围。

## 1. 合作指挥官独立 Mod 化

### 当前目标

将合作指挥官全部像斯台特曼一样，做成完全独立的指挥官 Mod：

- 每个指挥官有自己的 `CommanderUnits_<Name>.SC2Mod`。
- 每个指挥官 Mod 有 `DataCenter.json`。
- 每个指挥官 Mod 使用 `Base.SC2Data/GameData.xml <Includes>`。
- 每个指挥官 Mod 自带自己的 GameData 数据空间与 runtime galaxy。
- 后续可迁移到 `Mods/Commanders/<Commander>/<Commander>.SC2Mod`，脱离 7vs1 目录语义。

### 已完成证据

当前 `Mods/7vs1` 下已存在 19 个 `CommanderUnits_*.SC2Mod`：

| 指挥官 Mod | DataCenter | GameData Includes | GameData XML 数 | Galaxy 数 |
| --- | --- | --- | ---: | ---: |
| `CommanderUnits_Abathur.SC2Mod` | 有 | 有 | 7 | 4 |
| `CommanderUnits_Alarak.SC2Mod` | 有 | 有 | 1 | 1 |
| `CommanderUnits_Artanis.SC2Mod` | 有 | 有 | 1 | 1 |
| `CommanderUnits_Dehaka.SC2Mod` | 有 | 有 | 1 | 3 |
| `CommanderUnits_Fenix.SC2Mod` | 有 | 有 | 1 | 1 |
| `CommanderUnits_Horner.SC2Mod` | 有 | 有 | 2 | 1 |
| `CommanderUnits_Karax.SC2Mod` | 有 | 有 | 1 | 1 |
| `CommanderUnits_Kerrigan.SC2Mod` | 有 | 有 | 7 | 1 |
| `CommanderUnits_Mengsk.SC2Mod` | 有 | 有 | 1 | 3 |
| `CommanderUnits_Nova.SC2Mod` | 有 | 有 | 7 | 3 |
| `CommanderUnits_Raynor.SC2Mod` | 有 | 有 | 2 | 1 |
| `CommanderUnits_RaynorX.SC2Mod` | 有 | 有 | 5 | 1 |
| `CommanderUnits_Stetmann.SC2Mod` | 有 | 有 | 24 | 3 |
| `CommanderUnits_Stukov.SC2Mod` | 有 | 有 | 1 | 3 |
| `CommanderUnits_Swann.SC2Mod` | 有 | 有 | 1 | 4 |
| `CommanderUnits_TychusXM.SC2Mod` | 有 | 有 | 23 | 1 |
| `CommanderUnits_Vorazun.SC2Mod` | 有 | 有 | 1 | 1 |
| `CommanderUnits_Zagara.SC2Mod` | 有 | 有 | 1 | 1 |
| `CommanderUnits_Zeratul.SC2Mod` | 有 | 有 | 1 | 1 |

斯台特曼已经是最完整样板：

- `CommanderUnits_Stetmann.SC2Mod/DataCenter.json` 标记为 `Commander.Stetmann`、`CommanderDataCenter`、`migrationStatus: complete`。
- 使用 24 个 GameData 数据空间。
- 导出大量 `units`、`upgrades`、`buttons`、`requirements`。
- 有 `LibE0EAE146_StetmannRuntime.galaxy` 和原始库文件。
- `Shared/Commanders/Stetmann.json` 已有运行时身份清单，但仍是旧格式字段：`runtime_name`、`runtime_init`、`runtime_init_lib`。

### 当前进度判断

进度等级：**结构迁移大体完成，平台化独立尚未完成**。

已经完成的部分：

- 19 个官方/变体指挥官包均已具备独立 SC2Mod 容器。
- 19 个包均已有 `DataCenter.json`。
- 19 个包均已使用 `GameData.xml <Includes>`。
- Stetmann/TychusXM 属于高完整度样板，数据空间拆分较细。

仍未完成的部分：

- 大多数指挥官仍只有 1 个 GameData XML，实际只是“独立容器 + 单文件数据空间”，未达到 Stetmann 那种按 Catalog 类型全面拆分的状态。
- 包仍位于 `Mods/7vs1/CommanderUnits_*`，目录语义仍绑定 7vs1；尚未迁移到 `Mods/Commanders/<Commander>/`。
- `Mods/7vs1/CommanderPackages` 目前只看到 `TerranRaynor.json`，说明新的 CommanderPackage 层还没有覆盖全部指挥官。
- `Shared/Commanders/*.json` 与 `DataCenter.json` 之间的 schema 还未完全统一。
- 运行时仍可能依赖 `CoreRuntime` 全量 include 或 launcher 注入，不等同于“未选择指挥官不进入组合”。

### 后续建议

优先顺序：

1. 以 `Stetmann` 为 gold sample，定义“完全独立指挥官 Mod”的验收清单。
2. 把 `Raynor`、`Abathur`、`Kerrigan`、`Karax` 四个代表型指挥官补齐到 Stetmann 级别。
3. 为全部 19 个 `CommanderUnits_*` 生成/补齐 `CommanderPackage` 新格式。
4. 将目录语义从 `Mods/7vs1/CommanderUnits_*` 迁移到 `Mods/Commanders/<Commander>/`，先建立 alias/manifest，不急于物理搬迁。
5. 用 `sc2-composer` 增加检查：`Shared/Commanders/*.json`、`DataCenter.json`、实际 GameData/Galaxy 三方一致。
6. 用 `unitDiagnostics` 对每个指挥官至少验证一个生产链、一个升级链、一个 runtime 技能注入链。

## 2. Neuro 集成

### 当前目标

完成 Neuro 集成，使 7vs1 合作指挥官地图可叠加 Neuro-sama 运行时：

- 注入 `NeuroIntegration`。
- 注入 `NeuroBridge7vs1`。
- 追加依赖、Bank、MapScript/Galaxy include。
- 启动 Python runtime 或 Gary。
- Web launcher 能触发 Neuro 启动。

### 已完成证据

已存在核心文件：

| 文件 | 状态 |
| --- | --- |
| `Mods/Neuro/NeuroBridge7vs1.SC2Mod/DataCenter.json` | 存在，445 bytes |
| `Mods/Neuro/NeuroBridge7vs1.SC2Mod/Base.SC2Data/LibNeuroBridge7vs1.galaxy` | 存在，27,376 bytes |
| `Mods/Neuro/NeuroBridge7vs1.SC2Mod/Base.SC2Data/LibNeuroBridge7vs1_h.galaxy` | 存在，2,617 bytes |
| `scripts/launch-7vs1-neuro.ps1` | 存在，15,744 bytes |
| `web-launcher/routes/launch-neuro.mjs` | 存在，4,991 bytes |
| `logs/neuro-runtime.log` | 存在，101 bytes |

`launch-7vs1-neuro.ps1` 已实现薄包装思路：

- 调用 `launch-7vs1-coop-test.ps1 -NoLaunch` 完成 7vs1 依赖重写和地图安装。
- 对 live map 追加 `NeuroIntegration` 与 `NeuroBridge7vs1`。
- 注入 `BankList.xml`、`MapScript.galaxy`、bridge galaxy 库。
- 支持 mock Neuro URL、Gary 真实模式、自定义 URL。
- 有 `Invoke-FileOps` 辅助函数绕开 TRAE/Solo 对直接文件操作的拦截。

Web 侧已存在 `POST /api/neuro-launch`：

- 参数包含 commander、mapName、dryRun/noLaunch、skipPython、useGary。
- spawn `pwsh` 调用 `scripts/launch-7vs1-neuro.ps1`。
- 输出日志到 `logs/web-launcher-neuro-*.log`。

### 当前验证结果

已执行静态验证：

```text
node scripts/sc2-composer/cli.mjs lint-dataspaces --mod Mods/Neuro/NeuroBridge7vs1.SC2Mod --json
```

结果：**失败 1 error**。

```text
DC-002 id 格式无效: Neuro.Bridge7vs1，应为 <type>.<Name>
```

原因：

- 当前 `DataCenter.json` 使用：
  - `id: "Neuro.Bridge7vs1"`
  - `type: "PlatformDataCenter"`
- 现有 DataCenter schema 只允许 ID 前缀：
  - `Platform`
  - `Capability`
  - `Commander`
  - `MapFamily`
  - `PairPatch`

已执行 Galaxy 静态检查：

```text
node scripts/galaxy-checker/dist/cli.mjs Mods/Neuro/NeuroBridge7vs1.SC2Mod/Base.SC2Data --format text
```

结果：**0 错误，1 警告**。

```text
LibNeuroBridge7vs1_h.galaxy:2:1 XLIB_MISSING_INCLUDE
include "LibEFA54406_h" 未找到
```

判断：

- Galaxy 语法层面当前可过 checker。
- 缺失 include 可能来自外部 `NeuroIntegration` 依赖，不能直接当作致命错误，但必须在运行时依赖闭包中证明该库存在。
- DataCenter manifest 当前不合格，需要优先修正。

### 当前进度判断

进度等级：**功能骨架已完成，工程门禁未通过，不能声称完成集成**。

已经完成的部分：

- Mod 容器存在。
- Bridge Galaxy 存在。
- 启动脚本存在。
- Web route 存在。
- Galaxy checker 无 error。

阻塞/待修：

- `DataCenter.json` ID 不符合 schema。
- `LibEFA54406_h` 缺失 include 需要证明由 `NeuroIntegration` 依赖提供。
- 当前没有看到本次新鲜的进图验证证据。
- `logs/neuro-runtime.log` 只有 101 bytes，不足以证明 runtime 已完成交互闭环。
- `scripts/launch-7vs1-neuro.ps1` 当前为工作区未提交修改，需要区分已验证状态和正在修改状态。

### 后续建议

短期修复：

1. 将 `NeuroBridge7vs1.SC2Mod/DataCenter.json` 的 ID 改为 schema 合法值，例如：
   - `Platform.NeuroBridge7vs1`，或
   - 若更准确，可改为 `Capability.NeuroBridge7vs1`。
2. 跑 `lint-dataspaces --mod NeuroBridge7vs1`，确保 0 error。
3. 用依赖检查确认 `LibEFA54406_h` 来自 `NeuroIntegration`，并在 `DataCenter.json` 或 launch plan 中记录 import。
4. 对 `launch-7vs1-neuro.ps1 -NoLaunch` 做 dry run，确认依赖、Bank、MapScript、Galaxy 文件注入成功。
5. 再做一次实际进图，记录：
   - 启动命令。
   - SC2 进程退出状态。
   - `ScriptError.txt` 状态。
   - Neuro Python/Gary runtime 日志。
   - 游戏内至少一个 bridge 事件或 mock response。

中期工程化：

- 把 Neuro 从 7vs1 专用脚本迁到 CompositionPlan capability。
- Web route 不直接拼启动参数，而是请求 `/api/plan/preview` 或 plan runner。
- NeuroBridge 的 Bank/Galaxy 注入进入 GalaxyManifest 或 CompositionPlan。

## 3. Galaxy 工具迁移

### 当前目标

完成 `sc2-galaxy-toolkit` 改造迁移，把本地 Python 解析器能力迁移到 TypeScript 工具链：

- 深度 CatalogStore。
- 单位关系图 explorer。
- Galaxy AST runtime scanner。
- CLI 对齐 Python 版命令行。
- 后续接入主项目 `diagnose-unit`、`VerificationReport` 和 runtime/static 差异诊断。

### 已完成证据

工具仓库状态：

```text
E:\Code\MyMod\SC2\tools\sc2-galaxy-toolkit
branch: main...origin/main [ahead 4]
```

最近提交：

| Commit | 内容 |
| --- | --- |
| `26de87d` | 新增 CLI 入口，对齐 Python 版命令行接口 |
| `67000e0` | 集成 Galaxy 编译器实现运行时 API 调用提取 |
| `297a32c` | 新增单位关系图浏览器包 |
| `f2f96e5` | 新增 DeepCatalogStore 深度解析与合并语义 |
| `95d1ff8` | monorepo 迁移、vitest、eslint、bundle |

包结构证据：

- `packages/sc2-data/src/deep-store.ts` 存在。
- `packages/sc2-unit-explorer/src/explorer.ts` 存在。
- `packages/sc2-unit-explorer/src/galaxy-scanner.ts` 存在。
- `packages/sc2-unit-explorer/package.json` 依赖：
  - `sc2-data`
  - `sc2-galaxy-lang`
  - `sc2-mod`
- `packages/sc2-data/package.json` 依赖：
  - `sc2-mod`
  - `sc2-xml`
  - `vscode-languageserver-textdocument`
  - `vscode-uri`

已执行构建：

```text
pnpm -r run build
```

结果：**通过**。

所有相关包 build 均完成：

- `sc2-galaxy-lang`
- `sc2-mod`
- `sc2-xml`
- `sc2-data`
- `sc2-layout-lang`
- `sc2-trigger`
- `sc2-text`
- `sc2-lsp`
- `sc2-unit-explorer`
- `vscode-sc2-galaxy`

已执行测试：

```text
pnpm -r run test
```

结果：**失败 1 test**。

失败位置：

```text
packages/sc2-layout-lang/test/schema/localization.test.ts
schema localization > mdread > EAnimationEventNative
expected 'TBD\r\n\r\n' to equal undefined
```

判断：

- 当前失败来自 `sc2-layout-lang` 的 schema localization fixture/解析期望，不是 `sc2-unit-explorer` 迁移主线的直接失败。
- 但由于 monorepo 全量测试失败，不能写“全部测试通过”。
- `迁移进度.md` 中“测试状态：所有验证脚本通过”的说法需要更新为“构建通过，全量测试有 1 个既有/旁路包失败；迁移验证脚本需另行正式化”。

### 当前进度判断

进度等级：**核心迁移完成，工程收口未完成**。

已经完成的部分：

- DeepCatalogStore 已迁移。
- UnitExplorer 已迁移。
- GalaxyRuntimeScanner 已迁移。
- CLI 入口新增，本地分支领先 4 个提交。
- 构建全绿。

仍未完成的部分：

- 工具仓库 origin 只读，本地领先 4 个提交，尚未进入可协作 remote。
- 根目录仍有未跟踪测试脚本：
  - `test-data-mod.mjs`
  - `test-galaxy-lang.mjs`
  - `test-trigger.mjs`
- 全量测试失败 1 项。
- 迁移验证脚本仍是散落脚本，尚未统一进 vitest 或 package test。
- 尚未接入主项目 `sc2-editor-toolkit diagnose-unit`。
- 尚未把 runtime scanner 输出接入 `VerificationReport`。

### 后续建议

短期：

1. 修复或隔离 `sc2-layout-lang/test/schema/localization.test.ts` 的 `TBD` fixture 期望问题。
2. 把根目录 `test-*.mjs` / `test-*.py` 分类：
   - 正式回归：迁入 vitest。
   - 一次性对比脚本：移动到 `docs` 或 `scripts/manual/` 并标注。
3. 更新 `迁移进度.md`：
   - 分支领先应改为 4 个提交。
   - 新增 `26de87d` CLI 阶段。
   - 测试状态改为“build 通过，full test 当前 1 fail”。
4. 给工具仓库换可写 remote，或纳入主仓库 vendor/subtree 管理。

中期：

- 主项目 `diagnose-unit` 调用 `sc2-unit-explorer`。
- 让报告输出：
  - static Catalog。
  - effective Catalog。
  - runtime ability injection。
  - runtime tech allow/block。
  - confidence / complete。
- 把鸡翅战役科技不一致、Raynor Marine/Marauder/Vulture 等 case 变成正式 fixtures。

## 4. 三项任务总览

| 任务 | 当前状态 | 可声称完成的范围 | 不能声称完成的范围 | 下一步 |
| --- | --- | --- | --- | --- |
| 指挥官独立 Mod 化 | 结构迁移大体完成 | 19 个 `CommanderUnits_*` 均有 DataCenter + Includes | 还不是全部 Stetmann 级拆分，也未脱离 7vs1 目录语义 | 定义 Stetmann gold sample，补齐 CommanderPackage |
| Neuro 集成 | 骨架完成但门禁失败 | bridge mod、启动脚本、Web route 已存在；Galaxy 0 error | DataCenter lint 失败；未证明依赖闭包/进图/runtime 事件 | 修 `DataCenter.id`，验证 `LibEFA54406_h` 来源，跑 dry-run 和进图 |
| Galaxy 工具迁移 | 核心完成但工程收口未完成 | build 全绿；4 个本地提交覆盖 deep-store/explorer/scanner/CLI | full test 失败 1 项；未进入可写 remote；未接主项目 | 修 full test，正式化测试脚本，接入 diagnose-unit |

## 5. 关键风险

1. **“完成”定义混淆**：指挥官有独立 SC2Mod 不等于完全独立 CommanderPackage；Neuro 有启动脚本不等于运行时闭环；Galaxy 工具 build 通过不等于测试全绿。
2. **未提交/脏改动较多**：当前主仓和工具仓都有未跟踪或未提交内容，进度报告需要区分“本地存在”和“已稳定提交/推送”。
3. **验证等级不足**：Neuro 缺进图证据；指挥官独立化缺单位诊断矩阵；Galaxy 工具缺正式测试收口。
4. **跨仓库成果不可协作**：`sc2-galaxy-toolkit` origin 只读，本地 ahead 4，如果机器状态丢失会影响成果传递。

## 6. 建议下一轮执行顺序

1. 修 Neuro `DataCenter.id`，使 `lint-dataspaces --mod NeuroBridge7vs1` 通过。
2. 验证 `LibEFA54406_h` 的来源是否在 `NeuroIntegration` 依赖中，写入 manifest/依赖说明。
3. 对 Neuro 跑 `-NoLaunch` dry run，再做一次实际进图，收集 runtime 日志。
4. 修 `sc2-galaxy-toolkit` full test 失败项，并更新 `迁移进度.md`。
5. 把 `sc2-unit-explorer` 接入主项目 `diagnose-unit`。
6. 用 Stetmann gold sample 生成“完全独立指挥官 Mod 验收清单”，再批量评估 19 个指挥官。

## 7. 本次验证证据

执行过的验证：

- 主仓 `git status --short --branch`：确认当前分支 `fix_003`，存在多处其他任务脏改动。
- `CommanderUnits_*` 扫描：19 个指挥官包均有 DataCenter 和 GameData Includes。
- `NeuroBridge7vs1` 文件盘点：bridge、launcher、Web route、runtime log 均存在。
- `lint-dataspaces --mod NeuroBridge7vs1 --json`：失败 1 error，`DataCenter.id` 不符合 schema。
- `galaxy-checker NeuroBridge7vs1/Base.SC2Data --format text`：0 error，1 warning，缺 `LibEFA54406_h` include。
- `sc2-galaxy-toolkit git status/log`：工具仓 main ahead 4，最近提交包含 CLI 迁移。
- `pnpm -r run build` in `tools/sc2-galaxy-toolkit`：通过。
- `pnpm -r run test` in `tools/sc2-galaxy-toolkit`：失败 1 test，`sc2-layout-lang` localization 期望不一致。

