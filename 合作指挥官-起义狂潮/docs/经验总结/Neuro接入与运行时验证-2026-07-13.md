# Neuro 接入与运行时验证总结

- 任务类型：SC2 Neuro API 接入、Gary 真实服务联调、7vs1 进图验证
- 时间戳：2026-07-13 11:53:59 +08:00
- 任务内容：检查当前 Neuro 接入进度，补齐真实 Gary 服务地址、玩家操作上下文、玩家指令 force action、Neuro 动作执行和端到端验证。
- 任务结果：已完成真实 Gary 接入。`launch-7vs1-coop-test.ps1 -EnableNeuro -UseGary -Commanders TerranRaynor` 会连接 `C:\Users\22448\AppData\Local\Gary\gary.exe` 的 `ws://127.0.0.1:8000`，不再使用旧 `64998` 或 mock。Gary 日志确认收到资源、补给、单位、升级、生产建筑、经济与战斗上下文；真实 `actions/force` 进入 Gary 后，最终通过 Bank/Galaxy 执行 `train_unit(Marine)`，游戏侧返回 `OK: Ordered MarineRaynor via CommandCenterTrainRaynor[0] on 指挥中心.`。
- 任务耗时：约 2 小时。
- 任务备注：
  - `neuro-dependencies.json`、7vs1 coop launcher、Neuro launcher 的 Gary 默认 URL 更新为 `ws://127.0.0.1:8000`。
  - `neuro_bridge.py` 支持 Gary 将 `data` 作为 JSON 字符串返回，并对无效 action 参数返回失败结果，避免 WebSocket/桥接中断。
  - 为 `train_unit` 等动作补充 schema；`train_unit.arg_1` 使用 Raynor 常用生产单位枚举，其他单位/技能类参数使用 ID pattern。
  - Gary actor 可能在 `actions/force` 中给出合法但不符合玩家指令的枚举值，例如把 `train Marine` 漂移成 `Viking`；运行时和 `neuro_bridge.py` 均会从当前 `force_action` query/state 中提取明确参数并覆盖漂移，保证玩家指令优先。
  - 真实验证证据：Gary PID `38776` 监听 `127.0.0.1:8000`；Python integration 与 `neuro_bridge.py` 两个客户端建立真实 WebSocket；`wait-for-game-ready.ps1` exit 0；SC2 进程存活；启动后 60 秒内无新 ScriptError。
  - 最终 force 组：`codex_train_marine_51cdbc5b`。`NeuroIntegration.SC2Bank` 记录 `train_unit_arg_1=Marine` 和 `train_unit_result=OK: Ordered MarineRaynor via CommandCenterTrainRaynor[0] on 指挥中心.`。
  - 后续扩展建议：把同样的真实 Gary force 验证扩展到攻击、集火、集结点、移动、技能和科技研究动作，并覆盖更多 commander。
