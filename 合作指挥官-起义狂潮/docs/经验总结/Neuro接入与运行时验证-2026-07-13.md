# Neuro 接入与运行时验证总结

- 任务类型：SC2 Neuro API 接入、Galaxy 运行时改造、7vs1 进图验证
- 时间戳：2026-07-13 11:09:48 +08:00
- 任务内容：检查当前 Neuro 接入进度，补齐玩家操作读取、玩家指令上下文、Neuro 动作执行、运行时桥接和 7vs1 启动测试。
- 任务结果：已完成 Neuro bridge 扩展，能够注册并执行 13 个游戏侧动作，运行时合并 RuntimeProbe 后注册 18 个动作；游戏侧持续向 Neuro/mock 输出玩家单位、资源、补给、升级、生产队列、指挥官状态、战斗与经济变化等上下文。新增 `move_selected_to_unit` 写入型动作，并让玩家聊天进入 `player_directive` force action 决策链。
- 任务耗时：约 1 小时 15 分钟。
- 任务备注：
  - 修复 `launch-7vs1-coop-test.ps1 -EnableNeuro` 中 mock server stdout/stderr 同文件重定向导致的启动失败。
  - 修复 RuntimeProbe 中 `BankValueGetInt` 编译错误，并规避重复 UI Hookup 与 `DialogControlIsVisible` 运行时错误。
  - 修复 `research_upgrade` 对无效 Ability ID 构造 `AbilityCommand` 导致的 ScriptError，改为返回普通动作错误上下文。
  - 最终 7vs1 + Neuro 进图验证通过：`wait-for-game-ready.ps1` exit 0，SC2 进程存活，启动后未产生新的 ScriptError；mock 日志确认动作注册、上下文流、`get_mutators`、`get_commander_status`、`research_upgrade` 错误降级和 `move_selected_to_unit` 动作消费。
  - 自动 SendKeys 注入玩家聊天没有产生可用聊天事件证据；玩家聊天触发器与 `player_directive` force action 代码已接入，需后续人工游戏窗口输入或专用 UI 自动化补充端到端聊天证据。
