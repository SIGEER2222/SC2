# 任务总结：Neuro 副官完整接入

- 任务类型：SC2 7vs1 Neuro/Gary 真实服务接入、玩家指令 force action、端到端运行时验证
- 时间戳：2026-07-13 15:14:00 +08:00
- 任务内容：检查当前 Neuro 接入进度，直接使用本地 `SC2-Neuro-API-Integration` 与 `SC2-Neuro-WoL-Integration` 参考实现，补齐玩家聊天/指令到 Gary 的真实 force action 闭环，并验证 Gary 能把玩家命令转成单位操作。
- 任务结果：完成。现有 7vs1 NeuroBridge 已能监听玩家聊天、单位 order/ability/death、选择单位、经济与战斗快照，并注册 12 个副官动作；本轮补齐 force action 的 action 选择纠偏，避免 Gary 把明确命令误选为 `chat_message` 或错误单位动作。真实 Gary `ws://127.0.0.1:8000` 端到端验证通过，最终执行 `move_to_unit(SCVRaynor, CommandCenterRaynor)`，游戏侧返回 `OK: Ordered 12 SCVRaynor to move near CommandCenterRaynor.`。
- 任务耗时：约 1 小时 25 分钟。
- 任务备注：
  - 启动方式：`pwsh -NoProfile -ExecutionPolicy Bypass -File "合作指挥官-起义狂潮/scripts/launch-7vs1-coop-test.ps1" -EnableNeuro -UseGary -Commanders TerranRaynor`。
  - 等待方式：`pwsh -NoProfile -ExecutionPolicy Bypass -File "合作指挥官-起义狂潮/scripts/wait-for-game-ready.ps1"`，最后一次 exit 0；Alerts 出现，20 秒 grace 内无新 ScriptError，SC2 进程存活。
  - 真实服务证据：Gary PID `38776` 监听 `127.0.0.1:8000`，API integration 与项目 `neuro_bridge.py` 两个 Python 客户端均为 established 连接。
  - 动作证据：`NeuroIntegration.SC2Bank` 中 `do_action.move_to_unit=false`、`move_to_unit_arg_1=SCVRaynor`、`move_to_unit_arg_2=CommandCenterRaynor`，`game_context.move_to_unit_result=OK: Ordered 12 SCVRaynor to move near CommandCenterRaynor.`。
  - 验证：`python -m py_compile scripts/runtime-probe/neuro_bridge.py` 通过；`python -m pytest tests/test_force_action_arguments.py` 8/8 通过；Galaxy checker 对 NeuroBridge7vs1 为 0 error、1 个已知跨 Mod include warning。
  - 限制：用 SendKeys 向 SC2 窗口发送 `train Marine` 未能可靠触发实际聊天 UI 事件；已验证 Galaxy 侧存在 `TriggerAddEventChatMessage` 到 `player_chat_message/player_directive/force_action` 的链路，但本轮最终实机输入使用 Bank force action 注入来稳定验证真实 Gary 与游戏执行，不使用 mock。
