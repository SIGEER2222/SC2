# 地图已知问题

日期：2026-06-10

## Hanson 地图模型叠加未消失

涉及地图：

- `thanson02_7vs1.SC2Map`
- `thanson03a_7vs1.SC2Map`

现象：

- 进入地图后，盟友相关单位/演出单位存在模型叠加。
- 当前观察更像是地图原生演出单位、过场单位或预置单位与合作指挥官初始化共用了玩家槽位。
- 这不是单纯的“皮肤来回切换”。

已做尝试：

- 在共享库新增 `libE0EAE146_gf_SuppressPlayerCinematicUnits(mapId)`。
- 对 `thanson02_7vs1` 的 `gt_IntroHellions` 增加了演出单位抑制判断。
- 对 `thanson03a_7vs1` 的 `gt_BriefingScene02`、`gt_Briefing02VikingIndicators` 增加了演出单位抑制判断。

当前状态：

- 用户最新实测结论：问题仍未消失。
- 该问题先记录，后续单独排查，不阻塞当前共享化工作。

后续排查方向：

- 地图内预置单位是否直接占用了玩家/盟友槽位。
- actor 创建与单位创建是否存在分离，导致仅抑制造兵仍会残留模型。
- briefing / intro / victory 相关触发是否还有其他分支在创建同类单位。
- 是否存在地图对象、region 预摆单位、隐藏单位或 ownership 切换造成的叠加显示。
